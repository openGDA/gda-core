# Each row corresponds to an available theme with two columns
# Theme name - a-z and _ only
# Theme colour - comma separated theme styles
#                each style consists of one or two values separated by a semi-colon
#                corresponding to foreground colour and mask (bold/faint/italic
#                etc). If a single value is given it is assumed to be colour. If
#                no value is given, the default for the terminal is used.
#                eg 3;4 uses 3 for foreground and 4 for mask
#                   3 uses 3 for foreground and default mask
#                   ;4 uses default foreground and 4 for the mask
# Mask is a bit map:
#                BOLD = 1
#                FAINT = 1 << 1
#                ITALIC = 1 << 2
#                UNDERLINE = 1 << 3
#   KEYWORD, // - Python keywords eg for, if, def etc
#   NUMBER, // - Number literals eg 1, 23.45, 0x4B
#   STRING, // - String literals eg "helloWorld"
#   COMMENT, // - Commented code - anything following a #
#   OPERATOR, // - Signs and operators eg +, -, / etc
#   PUNCTUATION, // - Other punctuation eg (, ), "
#   DEFAULT, // - anything else

# Key
simple(9;1,5,10,8;6,,,)
