<?xml version="1.0" encoding="UTF-8"?>

<included>

    <!-- Include the standard set of properties files via the XML definition in dls-config -->
    <include file="${gda.facility.dir}/logging/logging_properties.xml"/>

    <!-- address where the messages from Object Servers send to - the LogServer-->
    <appender name="SOCKET" class="ch.qos.logback.classic.net.SocketAppender">
        <RemoteHost>${gda.logserver.host}</RemoteHost>
        <Port>${gda.server.logging.port}</Port>
        <ReconnectionDelay>10000</ReconnectionDelay>
        <IncludeCallerData>true</IncludeCallerData>
    </appender>

    <logger name="org.eclipse.jetty" level="INFO"/>

</included>
